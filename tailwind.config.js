/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './src/**/*.{html,js,jsx,ts,tsx}', // Tell Tailwind to scan all files in the `src` folder
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
